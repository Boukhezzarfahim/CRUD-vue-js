Small CRUD Application with Vue.js, Login System, and JSON Server API

This project is a simple CRUD application built with Vue.js that integrates a login system and communicates with a JSON Server API. The app allows users to perform basic operations such as creating, reading, updating, and deleting data. A login system is implemented to control access, and all data is stored and retrieved through a JSON Server running as a mock API.
